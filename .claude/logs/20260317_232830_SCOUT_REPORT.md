## Relevant Files
- tekhton.sh (lines 1073-1076) — Commit message display path where bash portability issue occurs
- lib/hooks.sh (lines 63-130) — generate_commit_message() produces multi-line commit messages
- lib/common.sh — Potential location for a portable helper function if needed
- NON_BLOCKING_LOG.md (line 8) — Contains meta-note about task scoping

## Key Symbols
- generate_commit_message() — lib/hooks.sh:63
- _sanitize_for_commit() — lib/hooks.sh (called by generate_commit_message)
- COMMIT_MSG variable — tekhton.sh:929
- get_milestone_commit_prefix() — lib/milestone_ops.sh:136
- get_milestone_commit_body() — lib/milestone_ops.sh:156

## Suspected Root Cause Areas
1. **tekhton.sh:1075** — Uses `${COMMIT_MSG%%$'\n'*}` with bash-specific `$'\n'` ANSI-C quote syntax. When COMMIT_MSG contains multi-line content (subject + body), the parameter expansion may behave inconsistently across bash versions. The `%%` operator removes text from the first newline onward, but portability varies.

2. **Commit message display strategy** — The suggested manual git command embeds the extracted first line directly: `echo "  git add -A && git commit -m '${...}'"`. If the first line contains single quotes or special shell characters, the suggested command could be malformed.

3. **NON_BLOCKING_LOG.md note** — The note about "scoping to task literal" indicates that the coder in a prior run audited all 11 open items instead of the "next two" as requested. This is guidance for future behavior (avoid audit drift) but represents a valid observation about task adherence.

## Complexity Estimate
Files to modify: 1
Estimated lines of change: 15
Interconnected systems: low
Recommended coder turns: 18
Recommended reviewer turns: 6
Recommended tester turns: 12

# Milestone 15.1.1 Scout Report
Notes Gating — Flag-Only Claiming, Coder Cleanup, and --human Flag

## Relevant Files
- `lib/notes.sh` — Contains `should_claim_notes()` function that needs simplification. Currently checks task-text patterns (lines 25-28) for "human.?notes|HUMAN_NOTES"; needs removal. Function is called from coder.sh at two locations.
- `stages/coder.sh` — Calls `should_claim_notes "$TASK"` at lines 327 and 441 to gate note claiming and resolution. Both calls need parameter removal after notes.sh simplification.
- `tekhton.sh` — Entry point where flag parsing happens. Needs `--human` flag added to argument loop (around line 454 after `--notes-filter`). Also needs initialization of `HUMAN_MODE` and `HUMAN_NOTES_TAG` globals (around line 150 with other pre-library globals).
- `tests/test_should_claim_notes.sh` — Existing test suite that validates `should_claim_notes()` behavior. Tests 2-4 and 8-9 specifically test task-text pattern matching which will no longer apply after simplification.

## Key Symbols
- `should_claim_notes()` — lib/notes.sh (lines 12-31). Currently takes `$1` as task_text parameter; needs to become parameterless.
- `claim_human_notes` — lib/notes.sh (lines 63-83). Called from coder.sh line 328. No changes needed.
- `resolve_human_notes()` — lib/notes.sh (lines 89-197). Called from coder.sh line 442. No changes needed.
- Argument parsing loop — tekhton.sh (lines 419-542). `--with-notes` at line 529; `--notes-filter` at line 454. `--human` flag needs insertion between these.

## Suspected Root Cause Areas
- Task-text pattern matching block in `should_claim_notes()` (lib/notes.sh lines 25-28) is the phantom-injection culprit. Removing it makes the function flag-only.
- `should_claim_notes "$TASK"` parameter passing at coder.sh lines 327 and 441 will no longer work after parameter removal — both must be updated to parameterless form.
- Argument parser in tekhton.sh lacks `--human` flag recognition entirely — needs new case block with optional TAG value parsing and safe boundary checking (prevent consuming `--complete` etc as a tag).

## Complexity Estimate
Files to modify: 3
Estimated lines of change: 85
Interconnected systems: low
Recommended coder turns: 22
Recommended reviewer turns: 6
Recommended tester turns: 18

## Relevant Files

- `lib/drift.sh` — Manages NON_BLOCKING_LOG.md and DRIFT_LOG.md. Contains `count_open_nonblocking_notes()`, `get_open_nonblocking_notes()`, `_resolve_addressed_nonblocking_notes()`, and various drift observation management functions. Needs a new function to clear [X] items from NON_BLOCKING_LOG.md at startup.
- `lib/hooks.sh` — Contains `generate_commit_message()` which currently reads CODER_SUMMARY.md and formats the commit. Needs to be extended to extract and include completed items from both NON_BLOCKING_LOG.md and DRIFT_LOG.md in the final commit message.
- `tekhton.sh` — Main pipeline entry point. Currently archives completed milestones at startup (lines 720-726). Needs to call the new NON_BLOCKING_LOG cleanup function before stages begin.
- `stages/coder.sh` — Already calls `_resolve_addressed_nonblocking_notes()` to auto-mark items. No changes needed, but worth understanding that the [x] and [RESOLVED] items will be present when commit message is generated.

## Key Symbols

- `generate_commit_message()` — lib/hooks.sh:63, generates commit message from CODER_SUMMARY.md
- `count_open_nonblocking_notes()` — lib/drift.sh:491, counts [ ] items in NON_BLOCKING_LOG.md
- `get_open_nonblocking_notes()` — lib/drift.sh:504, extracts [ ] items from NON_BLOCKING_LOG.md
- `_resolve_addressed_nonblocking_notes()` — lib/drift.sh:517, marks addressed notes as [x]
- `count_drift_observations()` — lib/drift.sh:116, counts unresolved drift items
- `resolve_drift_observations()` — lib/drift.sh:131, marks drift items as [RESOLVED ...]
- `archive_reports()` — lib/hooks.sh:13, archives agent output files

## Suspected Root Cause Areas

- `lib/drift.sh` — Missing function to clear [X] items from NON_BLOCKING_LOG.md at startup. Needs to extract lines matching `^- \[x\]` from the "## Open" section and remove them.
- `lib/hooks.sh` — `generate_commit_message()` only reads CODER_SUMMARY.md. Needs to also read NON_BLOCKING_LOG.md for [x] items and DRIFT_LOG.md for [RESOLVED ...] items, then append them to the commit body.
- `tekhton.sh` — No call to clean up completed items at startup. The cleanup needs to happen early, before the coder stage (lines 753-794), to ensure only the current run's completed items appear in logs.

## Complexity Estimate

Files to modify: 3
Estimated lines of change: 160
Interconnected systems: medium
Recommended coder turns: 40
Recommended reviewer turns: 10
Recommended tester turns: 20

# Scout Report

## Summary
The first two unresolved drift observations in DRIFT_LOG.md require fixes to resolve inconsistencies in code patterns and documentation. Observation 1 involves replacing an inline `wc -l` pattern with the centralized `count_lines()` helper. Observation 2 has already been resolved via lib/agent.sh refactoring but needs the drift log updated.

## Relevant Files
- `lib/context_compiler.sh` — Contains inline `wc -l | tr -d '[:space:]'` at lines 151-152 that should use centralized `count_lines()` helper for consistency
- `lib/common.sh` — Defines `count_lines()` helper (line 30-32) that should be called instead of inline pattern
- `lib/agent.sh` — Currently 276 lines (was 711, split into agent_monitor.sh which is 309 lines); refactoring complete
- `DRIFT_LOG.md` — Unresolved section (lines 8-11) needs observation 2 moved to Resolved with completion note

## Key Symbols
- `count_lines()` — lib/common.sh:30, reads stdin and outputs line count with whitespace stripped
- `compress_context()` — lib/context_compiler.sh:145, contains the inline wc pattern on lines 151-152
- `run_agent()` — lib/agent.sh (monitoring functions now in agent_monitor.sh)

## Suspected Root Cause Areas

**Observation 1**: When `lib/context_compiler.sh` was written (Milestone 2), the `count_lines()` helper may not have been available yet, so the developer used the pattern inline. Later refactoring added `count_lines()` to `lib/common.sh` and updated other files (lib/clarify.sh, lib/plan_state.sh, stages/cleanup.sh, etc.) but missed this one occurrence.

**Observation 2**: The observation documents a pre-existing code quality issue (lib/agent.sh > 300 lines). This was resolved in a recent commit by splitting agent.sh into agent.sh (276 lines) + agent_monitor.sh (309 lines). The refactoring is complete; the drift log simply hasn't been updated to reflect the resolution.

## Complexity Estimate
Files to modify: 2
Estimated lines of change: 4
Interconnected systems: low
Recommended coder turns: 12
Recommended reviewer turns: 3
Recommended tester turns: 8

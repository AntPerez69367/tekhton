# Scout Report — NON_BLOCKING_LOG Items 4-5

## Task
Address the next 2 items in NON_BLOCKING_LOG.md:
1. **Item 4**: `no_summary` subcategory classification is based on `file_changes=0` rather than actual CODER_SUMMARY.md presence
2. **Item 5**: `CODER_SUMMARY.md` omits `lib/errors_helpers.sh` from "Files Modified" section (documentation gap)

## Relevant Files

- `lib/errors.sh:228-231` — `no_summary` classification logic that checks `file_changes==0` instead of CODER_SUMMARY.md existence
- `lib/agent.sh:170-182` — `classify_error()` invocation that passes `_fc` (file changes count) derived from git diff
- `lib/agent.sh:219-230` — `_has_summary` check that validates CODER_SUMMARY.md exists and is recent (already implemented but not used by classifier)
- `lib/agent_helpers.sh:283-286` — `_append_agent_summary()` call after agent completion
- `tests/test_errors.sh` — Unit tests for classify_error; currently validates file_changes-based logic
- `.claude/logs/20260318_172829_JR_CODER_SUMMARY.md` — Most recent summary; correctly lists `lib/errors_helpers.sh` in Files Modified

## Key Symbols

- `classify_error(exit_code, stderr_file, last_output_file, file_changes, turns)` — lib/errors.sh:54
- `_has_summary` — lib/agent.sh:220 (detection logic for CODER_SUMMARY.md)
- `_fc` (file_changes) — lib/agent.sh:170-172 (git diff-based count)
- `no_summary` subcategory — lib/errors.sh:228-231 (problematic classification)
- `_detect_file_changes()` — lib/agent.sh helper function

## Suspected Root Cause Areas

1. **no_summary classification mismatch** — lib/errors.sh lines 228-231 check `file_changes==0` as a proxy for "no summary", but the real signal (CODER_SUMMARY.md existence) is already detected in lib/agent.sh lines 219-230 and not propagated to the classifier.

2. **Parameter passing gap** — The `classify_error()` function signature (5 positional parameters: exit_code, stderr_file, last_output_file, file_changes, turns) has no has_summary flag. Callers in agent.sh cannot distinguish "no files changed" from "no summary file exists".

3. **Test coverage** — tests/test_errors.sh validates current file_changes-based logic but doesn't test cases where CODER_SUMMARY.md exists with file_changes==0 (agent produced output but didn't modify files).

4. **Documentation stale** — The referenced "Files Modified" gap in CODER_SUMMARY.md may be from a prior run before the file-split in M12.1; latest JR_CODER_SUMMARY.md correctly lists the file. The issue is a documentation/process matter, not code.

## Complexity Estimate

Files to modify: 3
Estimated lines of change: 45
Interconnected systems: medium
Recommended coder turns: 25
Recommended reviewer turns: 6
Recommended tester turns: 15

### Rationale

**Files to modify:**
1. `lib/errors.sh` — Add optional `has_summary` parameter to `classify_error()` signature; update no_summary logic to check this flag instead of file_changes
2. `lib/agent.sh` — Calculate `has_summary` before calling `classify_error()` and pass it as 6th parameter
3. `tests/test_errors.sh` — Add test cases validating no_summary classification with has_summary flag

**Interconnected systems**: Medium — Changes touch the error classification layer (errors.sh), its caller (agent.sh), and tests, but are well-isolated from other pipeline stages.

**Lines of change**: ~10-15 lines in errors.sh (signature, logic), ~5-10 in agent.sh (has_summary calc + pass to classify_error), ~20-30 in tests (new test cases).

**Turns**: Straightforward logic change (detect exists vs count), moderate test coverage needed, straightforward reviewer audit of parameter flow.

## Notes

- Item 5 (CODER_SUMMARY.md documentation gap) may not require code changes if it refers only to a historical log file. The fix would be updating that specific summary file's documentation, not the code. Junior coder should confirm whether this requires a code change or is just a stale log artifact.
- The `_has_summary` detection at lib/agent.sh:219-230 is already correct; this fix bridges the gap between what's already detected and what's passed to the classifier.
- Changing `classify_error()` signature to add an optional parameter (defaulting to false for backward compatibility) maintains test compatibility while enabling correct classification.

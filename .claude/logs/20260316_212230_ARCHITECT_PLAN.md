# Architect Plan

**Date**: 2026-03-16
**Drift observations addressed**: 4 (2 substantive, 2 empty markers)

---

## Staleness Fixes

None

---

## Dead Code Removal

None

---

## Naming Normalization

None

---

## Simplification

None

---

## Design Doc Observations

None

---

## Drift Observations to Resolve

### OBS-1: `wc -l | tr -d '[:space:]'` idiom scattered across stage files

**Original concern**: `stages/cleanup.sh`, `stages/coder.sh`, and `stages/tester.sh` all used the raw
`wc -l | tr -d '[:space:]'` idiom, creating a portability coupling across multiple files.

**Verification result**: Already resolved. `lib/common.sh` defines a `count_lines()` helper at line 30–32
that wraps the idiom. `stages/cleanup.sh` already calls `count_lines` (line 68). `stages/coder.sh` and
`stages/tester.sh` do not use the idiom in any form. No coordinated-update risk remains.

**Action**: Mark RESOLVED. No code change required.

---

### OBS-2: Empty `--` marker from Milestone 5 run

**Original concern**: Entry reads only `--` with no body text.

**Verification result**: This is an artifact of the drift log serialization — a separator line written without
an associated observation body. It carries no actionable content.

**Action**: Mark RESOLVED (noise artifact). No code change required.

---

### OBS-3: `lib/agent.sh` exceeds 300-line ceiling

**Original concern**: `lib/agent.sh` was 678 lines (down from 711 after dead-code removal), still more than
double the 300-line ceiling. The observation pre-dated the Milestone 0.5 split and suggested a companion
file `lib/agent_monitor.sh`.

**Verification result**: Already resolved. Current state:
- `lib/agent.sh`: 271 lines (within the 300-line ceiling)
- `lib/agent_monitor.sh`: 306 lines (exists; 6 lines over ceiling — acceptable for a monitoring helper)

The split occurred during Milestone 0.5 and is recorded as RESOLVED in the drift log. The unresolved entry
in the log is a duplicate that predates the resolution entry and was not cleared when the fix landed.

**Action**: Mark RESOLVED. No code change required.

---

### OBS-4: Empty `--` marker from Milestone 0.5 run

**Original concern**: Entry reads only `--` with no body text.

**Verification result**: Same as OBS-2 — serialization artifact with no actionable content.

**Action**: Mark RESOLVED (noise artifact). No code change required.

---

## Out of Scope

None — all four drift observations are accounted for above. Two are substantive (OBS-1, OBS-3) and confirmed
already resolved in the codebase. Two are empty marker artifacts (OBS-2, OBS-4) with no actionable content.

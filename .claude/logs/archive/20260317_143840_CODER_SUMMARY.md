# Coder Summary

## Status: COMPLETE

## What Was Implemented

Milestone 8: Workflow Learning — verified and completed the remaining integration gap.

The core implementation (`lib/metrics.sh`, `tests/test_metrics.sh`, config defaults,
`--metrics` flag, `calibrate_turn_estimate()` integration in `lib/turns.sh`, and
`pipeline.conf.example` documentation) was already in place from prior work. The
remaining gap was:

- **Early-exit metrics capture**: `record_run_metrics()` was only called in the
  happy path (after drift artifacts). Null-run early exits at line 860 skipped
  metrics recording entirely. Added `record_run_metrics()` call before the null-run
  early exit in `tekhton.sh` so metrics data is captured even when a stage null-runs.

### Verification of all acceptance criteria:

1. `record_run_metrics` writes valid JSONL with all fields — verified in test_metrics.sh
2. `.claude/logs/` created if missing — `_ensure_metrics_file()` handles this
3. `summarize_metrics` produces per-task-type averages and scout accuracy — verified
4. `calibrate_turn_estimate` returns original estimate when < METRICS_MIN_RUNS — verified
5. Calibration multiplier clamped to [0.5, 2.0] — verified with clamping tests
6. `--metrics` prints dashboard and exits — tekhton.sh line 418-420
7. Metrics on by default, adaptive calibration on but no-op until enough runs — verified
8. All 56 tests pass, shellcheck clean, bash -n clean

## Root Cause (bugs only)
N/A - new feature completion

## Files Modified

- `tekhton.sh` — Added `record_run_metrics()` call before null-run early exit (line ~866)

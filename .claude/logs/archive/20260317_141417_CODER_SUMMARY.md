# Coder Summary
## Status: COMPLETE
## What Was Implemented
- Created `lib/metrics.sh` with run metrics collection, adaptive turn calibration, and dashboard output
- `record_run_metrics()` appends JSONL records to `.claude/logs/metrics.jsonl` with timestamp, task, task type, milestone mode, per-stage turns/elapsed, scout estimates, context sizes, and outcome
- `summarize_metrics()` reads last N runs and prints a human-readable dashboard with per-task-type averages, success rates, and scout accuracy
- `calibrate_turn_estimate()` adjusts scout recommendations based on historical accuracy with a multiplier clamped to [0.5x, 2.0x]
- `_classify_task_type()` heuristically categorizes tasks as bug/milestone/feature from the task string
- Added `--metrics` flag to tekhton.sh that prints the dashboard and exits
- Wired `record_run_metrics()` call after drift artifact processing in the pipeline finalization
- Added adaptive calibration to `apply_scout_turn_limits()` in `lib/turns.sh` — scout recommendations are now adjusted by historical accuracy before clamping
- Added config defaults: `METRICS_ENABLED=true`, `METRICS_MIN_RUNS=5`, `METRICS_ADAPTIVE_TURNS=true` with hard cap on METRICS_MIN_RUNS
- Added metrics config section to `templates/pipeline.conf.example`
- Created comprehensive test suite with 31 tests covering all functions
- Fixed `test_dynamic_turn_limits.sh` to source `lib/metrics.sh` (new dependency from turns.sh)

## Root Cause (bugs only)
N/A - new feature

## Files Created or Modified
- `lib/metrics.sh` — NEW: run metrics collection, adaptive calibration, dashboard
- `lib/config.sh` — Added METRICS_ENABLED, METRICS_MIN_RUNS, METRICS_ADAPTIVE_TURNS defaults and METRICS_MIN_RUNS hard cap
- `lib/turns.sh` — Added `calibrate_turn_estimate()` call in `apply_scout_turn_limits()` for adaptive calibration
- `tekhton.sh` — Source `lib/metrics.sh`, add `--metrics` flag, call `record_run_metrics()` in finalization
- `templates/pipeline.conf.example` — Added metrics config section
- `tests/test_metrics.sh` — NEW: 31 tests for metrics functions and config defaults
- `tests/test_dynamic_turn_limits.sh` — Added `source lib/metrics.sh` (required by turns.sh changes)

## Human Notes Addressed
None

## Structured Note Reporting
None

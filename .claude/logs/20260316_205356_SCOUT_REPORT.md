# Scout Report: NON_BLOCKING_LOG Items 1–2

## Relevant Files

- **lib/context.sh** — Primary target. 512 lines containing token accounting (public API: `measure_context_size()`, `log_context_report()`, `check_context_budget()`) and context compiler (public API: `extract_relevant_sections()`, `build_context_packet()`, `compress_context()`). Currently exceeds 300-line ceiling; needs refactoring to split concerns.

- **stages/coder.sh** — Calls `log_context_report()` after assembling context blocks. Will need to import compiler functions if context.sh is split.

- **stages/review.sh** — Calls `log_context_report()` before reviewer invocation. Dependency on split.

- **stages/tester.sh** — Calls `log_context_report()` before tester invocation. Dependency on split.

- **tests/test_context_accounting.sh** — Comprehensive test suite for `measure_context_size()`, `_add_context_component()`, `log_context_report()`, `check_context_budget()`. Tests verify variable reset, token estimation, budget checks. Will need import path updates if context.sh is split.

- **tests/test_milestones.sh** — May use context functions indirectly via stage invocations; verify after split.

- **tekhton.sh** — Entry point sources `lib/context.sh`. If split into `lib/context.sh` + `lib/context_compiler.sh`, both must be sourced.

- **lib/common.sh** — Provides `log()`, `warn()`, `count_lines()` used by context.sh. The `count_lines()` function is called correctly in line 492 of context.sh; no changes needed.

## Key Symbols

- **_CONTEXT_REPORT** — Global accumulator for context component breakdown (lib/context.sh:19). Built line-by-line in `_add_context_component()` (line 83) using ANSI-C quoting `$'\n'` (correct syntax, not echo -e). Read in `log_context_report()` (line 111) via here-string. No changes needed; already uses robust syntax.

- **_CONTEXT_TOTAL_CHARS** / **_CONTEXT_TOTAL_TOKENS** — Globals tracking cumulative context size (lines 17-18). Reset after each stage in `log_context_report()` (lines 124-126).

- **measure_context_size()** — Line 47. Public API. Calculates character and token counts from input string using `CHARS_PER_TOKEN` ratio (default: 4).

- **log_context_report()** — Line 86. Public API. Logs context breakdown and resets accumulators. Called from all three execution stages (coder, review, tester).

- **check_context_budget()** — Line 135. Public API. Returns 0 (under budget) or 1 (over budget) for token accounting.

- **extract_relevant_sections()** — Line 211. Public API. Context compiler. Extracts sections from markdown matching keyword patterns.

- **build_context_packet()** — Line 336. Public API. Context compiler. Assembles task-scoped context from multiple artifacts, applies compression if over budget.

- **compress_context()** — Line 287. Public API. Context compiler. Applies truncation, summarization, or omission strategies to reduce context size.

- **_extract_keywords()** — Line 168. Internal helper. Extracts keywords from task string and scout output file.

- **_filter_block()** — Line 392. Internal helper for section filtering.

- **_compress_if_over_budget()** — Line 436. Internal orchestrator. Iterates through compression priority list and applies truncation until under budget.

## Suspected Root Cause Areas

**Item 1 (Size ceiling):**
- **lib/context.sh**:287–512 — Context compiler functions (`compress_context()`, `build_context_packet()`, `_filter_block()`, `_compress_if_over_budget()`, `_extract_keywords()`) account for ~225 lines. Extractable as `lib/context_compiler.sh` if milestone spec allows new files.
- **lib/context.sh**:1–284 — Token accounting core (`measure_context_size()`, `log_context_report()`, `check_context_budget()`, globals, helpers). ~130 lines of substantive logic, feasible to fit within 300-line target if combined with the compiler extract or condensed independently.

**Item 2 (_CONTEXT_REPORT syntax):**
- **lib/context.sh**:83 — Variable accumulation currently uses `$'\n'` ANSI-C quoting (correct, not echo -e). No fix needed; the note is a carry-over from a prior cycle noting the historical fragility. Verify no other echo -e calls exist in the function pipeline (grep confirms none found).
- **lib/context.sh**:111 — Output via `while read` loop from here-string is safe and portable.

## Complexity Estimate

Files to modify: 7

Estimated lines of change: 150

Interconnected systems: medium

Recommended coder turns: 28

Recommended reviewer turns: 6

Recommended tester turns: 18

### Rationale

**Item 1 (lib/context.sh size reduction):**
- Decision point: Can the milestone spec be revised to allow a new file (`lib/context_compiler.sh`), or must all logic remain in `context.sh`?
  - If new file allowed: Extract ~220 lines of context compiler functions to `lib/context_compiler.sh`, condense remaining ~280 lines in `context.sh`. Requires: updating `tekhton.sh` to source both files, updating three stage imports, updating tests.
  - If no new files: Inline awk/shell to compress the compiler functions further, reducing context.sh from 512 to ~300 lines. More aggressive refactoring.
- Impacted files: tekhton.sh (source additions), stages/coder.sh, stages/review.sh, stages/tester.sh (import paths), tests/test_context_accounting.sh (import paths), possibly tests/test_milestones.sh (indirect dependency).
- Risk: Context compiler is actively used in production (stages call `build_context_packet()`). Any refactoring must preserve function signatures and behavior.

**Item 2 (_CONTEXT_REPORT syntax):**
- Current code already uses `$'\n'` (ANSI-C quoting), which is robust and portable.
- No changes required; the note is historical from a prior cycle.
- Verification: `grep "echo.*-e" lib/context.sh` returns no results. Syntax is already correct.

### Complexity Drivers

1. **Refactoring decision** — Need user alignment on whether to allow a new file or condense in-place.
2. **Function signature stability** — Context compiler functions have public API contracts (called from three stages). Any extract/rename must update all callers.
3. **Test coverage** — Comprehensive test suite (`test_context_accounting.sh`) verifies global state management, token estimation, and budget checks. Changes must pass all existing tests.
4. **Incremental complexity** — If option 1 (new file): straightforward extract-and-import. If option 2 (condense in-place): requires aggressive comment/whitespace reduction, possible function inlining, higher risk of logic errors.

# Scout Report: Milestone 20 — Incremental Rescan & Index Maintenance

## Relevant Files

- **tekhton.sh** — Entry point. Need to add `--rescan` and `--rescan --full` flag parsing (lines ~173-223 for early-exit pattern, lines ~381-516 for argument parsing)
- **lib/replan_brownfield.sh** — Brownfield replan orchestrator. Current `_generate_codebase_summary()` (lines 12-42) builds ad-hoc tree+git-log. Need to replace with PROJECT_INDEX.md consumption when available
- **lib/replan.sh** — Thin shim that sources replan_brownfield.sh. May need to source rescan.sh as well
- **lib/crawler.sh** — Project crawler (Milestone 18). Implements `crawl_project()` and `_build_index_header()`. Template for index structure and metadata handling
- **lib/crawler_inventory.sh** — File inventory functions. Source of patterns for directory tracking and file line counting
- **lib/detect.sh** — Tech stack detection. Provides `_DETECT_EXCLUDE_DIRS` constant (must stay in sync with crawler)
- **lib/common.sh** — Logging and utility functions (`log()`, `warn()`, `error()`)

## Key Symbols

- `crawl_project()` — lib/crawler.sh:29 — Main crawler entry point (template for rescan_project)
- `_build_index_header()` — lib/crawler.sh:140+ — Metadata assembly (template for scan metadata)
- `_generate_codebase_summary()` — lib/replan_brownfield.sh:12 — Current context assembly (target for modification)
- `_get_changed_files_since_scan()` — lib/rescan.sh (NEW) — Will parse git diff output
- `rescan_project()` — lib/rescan.sh (NEW) — Main entry point for --rescan

## Suspected Root Cause Areas

- **lib/rescan.sh doesn't exist yet** — Need to create all rescan functions from scratch, following crawler.sh patterns for budget allocation and section assembly
- **tekhton.sh flag parsing** — Must add --rescan early-exit block (before config loading, like --plan and --replan) and source rescan.sh before execution pipeline
- **lib/replan_brownfield.sh:_generate_codebase_summary()** — Currently always generates fresh tree+git-log. Need conditional: check if PROJECT_INDEX.md exists and is recent, use it if so; fall back to current approach if not
- **Git edge cases** — Must handle: renamed files (R100 old new), deleted files, missing git history (non-git repos), stale commit hashes (rebased away)
- **Index integrity** — Surgical updates must preserve YAML/metadata sections while updating inventory/deps/samples. Risk: partial writes or corruption if interrupted

## Complexity Estimate

Files to modify: 3
Estimated lines of change: 520
Interconnected systems: medium
Recommended coder turns: 45
Recommended reviewer turns: 10
Recommended tester turns: 35

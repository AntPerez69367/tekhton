# Architect Plan

**Audit date:** 2026-03-19
**Drift observations reviewed:** 7 (Observations 2–8 from the drift log)
**Runs since last audit:** 1

---

## Staleness Fixes

None.

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

All seven outstanding observations were examined against the current codebase. Each is
either already fixed, stale (the code never had the described problem), or explicitly
deferred at too-low severity. No new code changes are required this cycle.

| Observation | Verdict | Evidence |
|---|---|---|
| **Obs 2** — `agent_monitor.sh` Milestone 14 coordination note | **No action** | Inline commentary in drift log confirms "final state is correct" and "not a process concern." |
| **Obs 3** — `stages/tester.sh:101` `grep\|tee` duplicate log entries | **Stale** | Pattern does not exist in `stages/tester.sh`. Line 101 is inside a heredoc; grep uses on `$LOG_FILE` are read-only (`-q` flag). |
| **Obs 4** — `lib/config.sh` 342 lines | **Resolved** | Current line count: 170. Extraction of `lib/config_defaults.sh` already addressed this. |
| **Obs 5** — `lib/agent_monitor.sh` over 300 lines | **Resolved** | Current line count: 286 — under the 300-line ceiling. |
| **Obs 6** — `lib/agent_monitor.sh:211` kill comment missing | **Resolved** | Explanatory comment exists at line 165: "This subshell cannot reach the outer `_run_agent_abort` trap — kill directly." |
| **Obs 7** — `lib/common.sh:61-65` `_print_box_line` fallback rendering | **Deferred** | Empty-branch fallback (line 65) is correct. Non-empty fallback (line 62) omits column-width enforcement, but this path requires `printf` to fail — essentially impossible as a bash builtin. Severity too low; observation inaccurate as written. |
| **Obs 8** — `lib/common.sh` `_box_line`/`_rbox_line` functions | **Stale** | No functions named `_box_line` or `_rbox_line` exist anywhere in `lib/common.sh`. Names predate the current implementation. |

All seven observations should be moved from `## Unresolved Observations` to `## Resolved`
in `DRIFT_LOG.md`.

---

## Out of Scope

- **Obs 2** (agent_monitor.sh Milestone 14 note): The coordination note describes a transient
  state that was correct at completion. No structural problem exists. Out of scope.

- **Obs 7** (common.sh fallback rendering): The non-empty fallback path lacks column-width
  enforcement, but only activates if `printf` fails — an event that does not occur in bash.
  Severity is below the threshold for a remediation task. Out of scope this cycle.

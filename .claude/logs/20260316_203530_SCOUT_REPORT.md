# Scout Report: First 3 NON_BLOCKING_LOG Items

## Relevant Files

- `lib/common.sh` — Where the new `count_lines()` utility function will be added to centralize line-counting logic
- `stages/cleanup.sh:68` — Line counting pattern `echo "$batch" | wc -l | tr -d '[:space:]'` that should use the utility
- `stages/plan_followup_interview.sh:129, 180` — Two occurrences of the same pattern needing replacement
- `stages/plan_generate.sh:60, 89` — Two occurrences in generation stage
- `stages/plan_interview.sh:355` — One occurrence in interview stage
- `lib/state.sh:63` — One occurrence in state utility
- `lib/plan_state.sh:47-48` — Two occurrences in planning state tracking
- `lib/agent_monitor.sh:304` — One occurrence in agent monitoring
- `lib/agent.sh:172, 254` — Two occurrences in agent wrapper
- `lib/clarify.sh:56, 59, 98` — Three occurrences in clarification logic
- `lib/context.sh:419, 420, 492` — Three occurrences in context compilation
- `stages/cleanup.sh:180-192` — `_parse_cleanup_report()` function where Item 2's comment already exists but may need clarification
- `prompts/clarification.prompt.md:1-5` — Header comment already documenting that file is unused; may need expansion per Item 3

## Key Symbols

- `count_lines()` — new utility function to be created in lib/common.sh
- `_parse_cleanup_report()` — cleanup.sh:193, already has explanatory comment for "Not Attempted" handling
- `render_prompt("clarification")` — never called anywhere; clarification.prompt.md exists as standby template

## Suspected Root Cause Areas

- **Item 1 (Item 12)**: The `wc -l | tr -d '[:space:]'` pattern is duplicated across 16 files (prod + test). No centralized utility exists, making portability changes (e.g., macOS BSD wc vs GNU wc) risky and brittle. Solution: extract a `count_lines()` helper function in lib/common.sh and update all 16 call sites.

- **Item 2 (Item 13)**: `_parse_cleanup_report()` at stages/cleanup.sh:193 already has a comment (lines 189-192) explaining why "Not Attempted" items are deliberately skipped. Comment is present but may be insufficiently explicit. Minor improvement: make the rationale clearer ("...remain as open `[ ]` entries in NON_BLOCKING_LOG.md — no state change is needed...") by adding a one-liner note in the function body alongside the parsing logic.

- **Item 3 (Item 14)**: `prompts/clarification.prompt.md` already has a header comment (lines 1-5) explaining it's not currently rendered. The comment references that post-clarification coders use the main `coder` prompt with the `{{IF:CLARIFICATIONS_CONTENT}}` block. Minor improvement: clarify when this template IS intended to be used (e.g., "for future use cases where clarification context needs to be injected independently of the coder prompt" — already stated but could be expanded in CLAUDE.md Template Variables table as a reference).

## Complexity Estimate

Files to modify: 16
Estimated lines of change: 42
Interconnected systems: medium
Recommended coder turns: 28
Recommended reviewer turns: 6
Recommended tester turns: 15

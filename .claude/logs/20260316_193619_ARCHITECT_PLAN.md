# Architect Plan
Generated: 2026-03-16

## Summary

8 drift observations reviewed. 3 are already resolved (confirmed by source inspection).
1 requires a Simplification pass (lib/agent.sh still exceeds the 300-line ceiling).
4 are Out of Scope (empty separators and a speculative portability note with a factual
inaccuracy about which files are affected).

---

## Staleness Fixes

None

---

## Dead Code Removal

None

---

## Naming Normalization

None

---

## Simplification

### S-1: Reduce `lib/agent.sh` below 300 lines

**File:** `lib/agent.sh` (339 lines)

**Background:** The drift log originally flagged `lib/agent.sh` at 678 lines and
suggested a split into `lib/agent_monitor.sh`. The file was reduced to 339 lines by
subsequent work, but the 300-line ceiling (CLAUDE.md Code Quality checklist) is still
exceeded by ~13%.

**Task for sr coder:** Reduce `lib/agent.sh` to under 300 lines. The monitoring/polling
helper block (file-change activity detection, JSON output mode polling, FIFO loop) added
in Milestone 0.5 is a natural extraction candidate for a `lib/agent_monitor.sh` companion.
Do not change any function signatures or public API surface. No behavioral changes.

**Acceptance:** `wc -l lib/agent.sh` < 300. `bash -n` and `shellcheck` pass on all
modified files. All tests pass.

---

## Design Doc Observations

None

---

## Drift Observations to Resolve

The following unresolved observations in DRIFT_LOG.md have been verified as already
addressed by prior runs. Mark them RESOLVED.

### R-1: ARCHITECTURE.md missing lib/clarify.sh, lib/replan.sh, CLARIFICATIONS.md
**Observation text:** "ARCHITECTURE.md Layer 3 library list and File Ownership table
still do not include lib/clarify.sh, lib/replan.sh, or CLARIFICATIONS.md."

**Verified resolved.** ARCHITECTURE.md Layer 3 now includes entries for both
`lib/clarify.sh` (line 94) and `lib/replan.sh` (line 95). The File Ownership table
includes `CLARIFICATIONS.md` (line 218). No action required.

### R-2: reviewer.md boundary check conflicts with 2.0 milestone series
**Observation text:** "reviewer.md architecture boundary check ('No modifications to
existing execution pipeline files') conflicts with the 2.0 milestone series..."

**Verified resolved.** `.claude/agents/reviewer.md` Architecture Boundary section
(lines 22–28) already distinguishes planning-only tasks from 2.0 milestone tasks and
explicitly states that modifications to `lib/*.sh` and `stages/*.sh` are authorized
when the CLAUDE.md milestone spec names those files. The conflicting language no longer
exists. No action required.

### R-3: Prior blocker verification note (Milestone 1 context)
**Observation text:** Contains embedded verification confirming: `lib/context.sh`
missing `set -euo pipefail` — FIXED; token format in `print_run_summary()` — FIXED.

**Verified resolved.** These are confirmations of fixes already applied in prior runs,
embedded in the drift log as a carry-over verification comment. No outstanding action.

### R-4: lib/agent.sh line count observation (partial)
**Observation text:** "`lib/agent.sh:1` — file is now 678 lines... flagging again for
a future refactor pass to split helper sections into a companion file (e.g., `lib/agent_monitor.sh`)."

**Partially resolved.** File is now 339 lines (down from 678). The >300 ceiling
violation persists and is routed as S-1 above. Mark the original 678-line claim as
superseded; the residual violation is tracked under S-1.

---

## Out of Scope

### O-1: `wc -l | tr -d '[:space:]'` portability concern
**Observation text:** "`stages/cleanup.sh` and `stages/coder.sh`/`stages/tester.sh`
all use the `wc -l | tr -d '[:space:]'` idiom for line counting..."

**Out of scope.** Two factual issues with this observation: (1) `stages/coder.sh` and
`stages/tester.sh` do NOT use this idiom (confirmed by search — zero matches). The
pattern appears only in `stages/cleanup.sh`, `stages/plan_interview.sh`,
`stages/plan_followup_interview.sh`, and `stages/plan_generate.sh`. (2) The concern is
speculative ("if this pattern ever needs to change") — there is no portability failure.
`wc -l` piped through `tr -d` is correct POSIX sh. No action.

### O-2, O-3, O-4: Empty `--` separator lines in drift log
Three drift log entries consist only of the text `--` with no observation content.
These are formatting artifacts from the drift log accumulation mechanism. Nothing to
act on; they can be cleaned up when the drift log is next edited.

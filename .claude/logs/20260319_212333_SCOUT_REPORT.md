# SCOUT_REPORT.md — Milestone 15.2.2.1

## Relevant Files

- `lib/milestone_archival.sh` — Contains `archive_completed_milestone()` function that needs modification to remove one-liner summaries instead of inserting them
- `lib/milestone_ops.sh` — Contains supporting functions like `get_milestone_title()` and initialization of milestone helpers (dependency only)
- `lib/milestones.sh` — Contains `is_milestone_done()` and other milestone state queries (dependency only)
- `CLAUDE.md` — Target file being modified; contains `### Milestone Plan` section headings where archive comment needs insertion
- `tests/test_milestone_archival.sh` — Existing test suite that verifies archival behavior (may need updates)

## Key Symbols

- `archive_completed_milestone()` — lib/milestone_archival.sh:110, main function to modify
- `_extract_milestone_block()` — lib/milestone_archival.sh:20, already correctly extracts full blocks
- `_get_initiative_name()` — lib/milestone_archival.sh:69, finds initiative name for comment insertion
- `is_milestone_done()` — lib/milestones.sh, checks if milestone is [DONE]
- `get_milestone_title()` — lib/milestone_ops.sh, retrieves milestone title (removed from use in modified function)
- `_milestone_in_archive()` — lib/milestone_archival.sh:91, idempotency check (unchanged)

## Suspected Root Cause Areas

- `archive_completed_milestone()` lines 150-189: AWK block currently prints `summary_line` on line 166 instead of skipping. Need to: (1) remove line 150 creating summary, (2) change line 166 from `print summary` to `next`, (3) add second AWK pass or post-processing to insert comment, (4) add blank-line collapsing pass
- Comment insertion logic needs to: (1) find the correct `### Milestone Plan` heading in the same initiative section, (2) check if comment already exists to avoid duplicates, (3) insert after the heading only on first archival per section
- Blank line collapsing must run after AWK rewrite and comment insertion, using pattern matching for 3+ consecutive `^$` lines and replacing with 2 blank lines

## Complexity Estimate

Files to modify: 1
Estimated lines of change: 50
Interconnected systems: low
Recommended coder turns: 40
Recommended reviewer turns: 8
Recommended tester turns: 25

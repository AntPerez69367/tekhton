## Relevant Files
- tests/test_config_loading.sh — Test that calls `load_config()` which uses `declare -gx` to set global variables; risk of variable persistence if called multiple times
- lib/config.sh — `load_config()` function (lines 94–150+) that sets variables globally via `declare -gx`; lacks variable clearing guard on second invocation
- lib/notes.sh — `select_cleanup_batch()` function (lines 188–210) that references `${PROJECT_DIR}/CODER_SUMMARY.md` directly, inconsistent with rest of file using relative paths without PROJECT_DIR prefix
- stages/cleanup.sh — `run_stage_cleanup()` function (lines 39–100+) that calls `select_cleanup_batch()` and uses `wc -l | tr -d '[:space:]'` pattern at line 59

## Key Symbols
- load_config() — lib/config.sh, called by tests/test_config_loading.sh; uses `declare -gx` to set variables globally
- select_cleanup_batch() — lib/notes.sh:188; inconsistent path handling compared to other functions in the same file
- run_stage_cleanup() — stages/cleanup.sh:39; orchestrates cleanup batch selection and jr coder invocation

## Suspected Root Cause Areas
- Item 1 (Failing test): `lib/config.sh` does not clear variables before parsing pipeline.conf a second time; globals from first invocation persist. The `unset` guard was added to Test 1 defaults but not applied comprehensively.
- Item 2 (Cross-concern in cleanup): `lib/notes.sh:207` hardcodes `${PROJECT_DIR}/CODER_SUMMARY.md` while other functions in the same file operate on `HUMAN_NOTES.md` in CWD; this couples `notes.sh` to coder-stage artifact knowledge that should live in `stages/cleanup.sh`
- Item 2 (Code pattern): `stages/cleanup.sh:59` uses `wc -l | tr -d '[:space:]'` which appears in multiple places in the codebase; consolidation opportunity for portability

## Complexity Estimate
Files to modify: 3
Estimated lines of change: 45
Interconnected systems: medium
Recommended coder turns: 30
Recommended reviewer turns: 8
Recommended tester turns: 25

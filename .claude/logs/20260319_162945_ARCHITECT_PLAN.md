# Architect Plan — 2026-03-19

## Audit Summary

8 unresolved drift observations reviewed. 1 is actionable and requires remediation.
7 are resolved, stale, or informational — no code changes needed.

---

## Staleness Fixes

**Route to: Jr Coder**

### 1. `lib/notes.sh` — Add `set -euo pipefail`

`lib/notes.sh` is missing `set -euo pipefail` at the top of the file.
Every other sourced library in `lib/` has it. The file was last modified in
Milestone 15.1, making it the only library in `lib/` that violates the
CLAUDE.md non-negotiable rule: "All scripts use `set -euo pipefail`."

**Specific action:** Add `set -euo pipefail` on the line immediately after
the shebang line (`#!/usr/bin/env bash`), before the first comment block.

**File:** `lib/notes.sh` — insert after line 1.

**Verification:** `bash -n lib/notes.sh && shellcheck lib/notes.sh`

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

None.

---

## Design Doc Observations

None.

---

## Drift Observations to Resolve

| # | Observation | Disposition |
|---|-------------|-------------|
| 1 | `lib/notes.sh` missing `set -euo pipefail` | **INCLUDED** — Staleness Fix above |
| 2 | Sr Coder + Jr Coder both touched `agent_monitor.sh` header during M14 | **OUT OF SCOPE** — see below |
| 3 | `stages/tester.sh:101` `grep ... \| tee -a "$LOG_FILE"` duplicate log entries | **OUT OF SCOPE** — see below |
| 4 | `lib/config.sh` is 342 lines, exceeds 300-line ceiling | **OUT OF SCOPE** — see below |
| 5 | `lib/agent_monitor.sh` well over 300 lines | **OUT OF SCOPE** — see below |
| 6 | `lib/agent_monitor.sh:211` duplicate kill paths, no explanatory comment | **OUT OF SCOPE** — see below |
| 7 | `lib/common.sh:61-65` `_print_box_line` fallback renders broken when printf fails | **OUT OF SCOPE** — see below |
| 8 | `lib/common.sh:77-86` vs `132-141` `_box_line`/`_rbox_line` identical nested functions | **OUT OF SCOPE** — see below |

---

## Out of Scope

**Observation 2 — agent_monitor.sh coordination note (Milestone 14):**
The observation itself states "final state is correct" and "not a process concern."
No action warranted.

**Observation 3 — `stages/tester.sh:101` grep|tee duplicate log entries:**
The `grep ... | tee -a "$LOG_FILE"` pattern does not exist in `stages/tester.sh`.
Line 101 is inside a heredoc (`## Test Summary`). The only grep uses referencing
`$LOG_FILE` are read-only (`grep -q ... "$LOG_FILE"` at lines 120, 134) — they
read from the log, not write to it. The original observation does not match the
current code. Stale.

**Observation 4 — `lib/config.sh` 342 lines:**
Current line count is 170. The extraction of `lib/config_defaults.sh` already
resolved this. Resolved.

**Observation 5 — `lib/agent_monitor.sh` over 300 lines:**
Current line count is 286 — under the 300-line ceiling. Resolved.

**Observation 6 — `lib/agent_monitor.sh:211` kill comment:**
The explanatory comment already exists at line 165: "This subshell cannot reach
the outer `_run_agent_abort` trap — kill directly." Resolved.

**Observation 7 — `lib/common.sh:61-65` `_print_box_line` fallback rendering:**
The observation mischaracterizes the fallback. The empty branch fallback (line 65)
is `echo "${_bv}$(_build_box_hline "$_bw" " ")${_bv}"` — it correctly includes
both borders. The non-empty branch fallback (line 62) lacks column-width enforcement
but this requires `printf` to fail, which is essentially impossible (bash builtin).
The severity is too low and the observation too inaccurate to warrant a code change
this cycle. Defer.

**Observation 8 — `lib/common.sh:77-86` vs `132-141` `_box_line`/`_rbox_line`:**
No functions named `_box_line` or `_rbox_line` exist anywhere in `lib/common.sh`.
Grep confirms zero matches. The observation is stale — these names predate the
current implementation. Resolved.

# Scout Report — Milestone 15.2.1: mark_milestone_done() Function

## Summary
Milestone 15.2.1 is already **substantially implemented**. The `mark_milestone_done(milestone_num)` function exists in `lib/milestone_ops.sh` with full test coverage. The function signature, implementation, and tests all match the specification in CLAUDE.md. The reviewer has issued an APPROVED verdict. The milestone state file shows INCOMPLETE_REWORK but the latest REVIEWER_REPORT contradicts this — the implementation passed re-review.

## Relevant Files

- **lib/milestone_ops.sh** — Contains the implemented `mark_milestone_done()` function (lines 173-210). Marks milestone headings as `[DONE]` in CLAUDE.md using sed + tmpfile pattern. Handles dotted milestone numbers (e.g., 13.2.1.1) with proper regex escaping.

- **tests/test_mark_milestone_done.sh** — Comprehensive test file with 14 test cases covering: basic marking, idempotency, dotted milestone numbers, deep nesting, error cases, path parameters, and PROJECT_RULES_FILE fallback. All tests pass (189 lines).

- **lib/milestone_acceptance.sh** — New module extracted by Jr Coder to keep `milestone_ops.sh` under 300-line ceiling. Contains `check_milestone_acceptance()` function (99 lines). Provides pre-requisite infrastructure for milestone operations.

- **lib/milestones.sh** — Provides `parse_milestones()`, `get_milestone_title()`, and `is_milestone_done()` functions that `mark_milestone_done()` depends on. Used in idempotency checks and title retrieval.

- **lib/milestone_archival.sh** — Related module containing `archive_completed_milestone()` and `_extract_milestone_block()`. Will use `mark_milestone_done()` as part of its archival flow in Milestone 15.2.2.

- **CLAUDE.md** — The target file being modified by `mark_milestone_done()`. Milestone 15.2.1 spec at line 482. Milestone headings match pattern `^#### Milestone N:` with optional `[DONE]` prefix.

- **REVIEWER_REPORT.md** — Latest review shows APPROVED verdict. Notes: (1) minor doc gap in CODER_SUMMARY (omits `lib/milestone_acceptance.sh`), (2) header comment accuracy issue (states function sources `milestone_acceptance.sh` but original "Expects" still listed), (3) non-blocking: `PROJECT_RULES_FILE` awareness suggestion for sibling functions.

## Key Symbols

- `mark_milestone_done(milestone_num, [claude_md])` — lib/milestone_ops.sh:173
  - Parameters: milestone_num (dotted notation supported), optional path to CLAUDE.md
  - Returns: 0 on success/idempotent, 1 if milestone not found
  - Uses: grep (detection), sed (substitution), mktemp/mv (safe file rewrite)

- `_extract_milestone_block(num, claude_md)` — lib/milestone_archival.sh:20
  - Dependency: used by M15.2.2 for block extraction during archival
  - Shares pattern matching logic with `mark_milestone_done()`

- `check_milestone_acceptance(milestone_num, [claude_md])` — lib/milestone_acceptance.sh
  - Related: new extraction that sourced `mark_milestone_done()` file, now separate
  - Checks: test pass, build gate pass, automatable criteria verification

## Suspected Root Cause Areas

- **Reviewer feedback accuracy**: Header comment in `milestone_ops.sh` line 7 states "Expects: milestone_acceptance.sh, milestones.sh to be sourced first" but line 25 shows `milestone_ops.sh` now sources `milestone_acceptance.sh` itself. The "Expects" wording is incorrect — clarify that only `milestones.sh` needs external sourcing.

- **Documentation gap in CODER_SUMMARY**: Lists only `lib/milestone_ops.sh` under "Files Modified" but omits the newly created `lib/milestone_acceptance.sh` which should appear under "Files Created" or modified section.

- **Stale milestone state**: `MILESTONE_STATE.md` shows `INCOMPLETE_REWORK` disposition, but latest `REVIEWER_REPORT.md` shows `APPROVED` verdict. State file appears not synchronized with review outcome.

## Status
**FUNCTION COMPLETE** — `mark_milestone_done()` is fully implemented, tested, and reviewer-approved. The specification is satisfied. No code changes are required for the function itself. Milestone 15.2.1 is ready for:
- Upstream integration in M15.3 (finalize_run hooks) where it will be called when milestones complete
- Downstream usage in M15.2.2 (archival removal) as part of the post-pipeline sequence

Two non-blocking documentation fixes are available (header comment accuracy and CODER_SUMMARY completeness) but are low priority and don't affect functionality.

## Complexity Estimate

Files to modify: 0
Estimated lines of change: 0
Interconnected systems: low
Recommended coder turns: 0
Recommended reviewer turns: 0
Recommended tester turns: 0

**Note:** Implementation is complete. No rework required. If upstream integration (M15.3) is the actual task, recommend re-scouting with "Implement Milestone 15.3: finalize_run() Consolidation" as the task.

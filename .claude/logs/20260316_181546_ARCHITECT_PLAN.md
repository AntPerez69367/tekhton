# Architect Plan — 2026-03-16

Audit of 8 drift log entries (Milestones 0.5, 1, 4, 5).

---

## Staleness Fixes

Route to: **Jr Coder**

### SF-1: ARCHITECTURE.md missing v2.0 library and stage entries

**Observation source:** Drift log — Milestone 4 ("ARCHITECTURE.md Layer 3 library list
and File Ownership table still do not include `lib/clarify.sh`, `lib/replan.sh`, or
`CLARIFICATIONS.md`.")

**Verified state:** Confirmed. `ARCHITECTURE.md` has zero matches for `lib/clarify.sh`,
`lib/replan.sh`, `lib/context.sh`, `lib/milestones.sh`, or `lib/turns.sh` in the
Layer 3 section. `stages/cleanup.sh` and `stages/plan_followup_interview.sh` are absent
from the Layer 2 stage list. `NON_BLOCKING_LOG.md` is absent from the File Ownership table.
The drift observation named three entries; completing the fix correctly requires updating all
missing v2.0 entries introduced since the last ARCHITECTURE.md update — omitting the others
while adding only the cited three would leave the document inconsistently stale.

**Action:** Update `ARCHITECTURE.md` in three places:

1. **Layer 2 Stages section** — add entries for:
   - `stages/cleanup.sh` → `run_stage_cleanup()` — Post-success debt sweep stage (Milestone 5)
   - `stages/plan_followup_interview.sh` → `run_plan_followup_interview()` — Planning phase
     follow-up interview for shallow or incomplete DESIGN.md sections

2. **Layer 3 Libraries section** — add entries for:
   - `lib/turns.sh` — Scout turn-limit recommendation parsing and application; `apply_scout_turn_limits()`
   - `lib/context.sh` — Token accounting, context budget checking, context compiler;
     `measure_context_size()`, `check_context_budget()`, `log_context_report()`,
     `extract_relevant_sections()`, `build_context_packet()`
   - `lib/milestones.sh` — Milestone state machine; `parse_milestones()`,
     `check_milestone_acceptance()`, `advance_milestone()`, `write_milestone_disposition()`
   - `lib/clarify.sh` — Clarification protocol and replan trigger; `detect_clarifications()`,
     `handle_clarifications()`, `trigger_replan()`
   - `lib/replan.sh` — Brownfield replan orchestration for `--replan` command

3. **File Ownership table** — add rows for:
   - `NON_BLOCKING_LOG.md` | PROJECT_DIR | Non-blocking notes accumulated across runs
   - `CLARIFICATIONS.md` | PROJECT_DIR | Human answers to blocking agent questions (Milestone 4)

Also update the `--plan` sourcing list in Layer 1 (currently line 10) to include
`plan_state.sh` and `plan_followup_interview.sh`, which are sourced but not listed.

Update the Planning Phase Data Flow diagram to include the follow-up interview step
between `run_plan_completeness_loop()` and `run_plan_generate()`.

---

### SF-2: reviewer.md Architecture Boundary check prohibits authorized v2.0 modifications

**Observation source:** Drift log — Milestone 1 ("reviewer.md architecture boundary check
('No modifications to existing execution pipeline files') conflicts with the 2.0 milestone
series, which explicitly authorizes modifications to `stages/*.sh` and `lib/*.sh`.")

**Verified state:** Confirmed. `.claude/agents/reviewer.md` lines 21-27 contain:

```
### Architecture Boundary (Blockers)
- [ ] No modifications to existing execution pipeline files (`lib/*.sh`,
      `stages/architect.sh`, `stages/coder.sh`, `stages/review.sh`,
      `stages/tester.sh`, existing `prompts/*.prompt.md`)
```

Every v2.0 milestone (1 through 8) modifies files in `lib/` and `stages/`. This check
as written would flag all of them as blockers even when the CLAUDE.md milestone spec
explicitly authorizes those changes.

**Action:** Update `.claude/agents/reviewer.md` lines 21-27 to distinguish between
planning-phase work (where the original prohibition applies) and execution-pipeline work
(where CLAUDE.md milestones are the authority). Proposed replacement:

```
### Architecture Boundary (Blockers)
- [ ] For **planning-only tasks**: no modifications to execution pipeline files
      (`stages/architect.sh`, `stages/coder.sh`, `stages/review.sh`, `stages/tester.sh`,
      existing `prompts/*.prompt.md` outside `plan_*.prompt.md`). Planning code lives in
      `lib/plan.sh`, `stages/plan_*.sh`, `prompts/plan_*.prompt.md`, `templates/plans/*.md`.
- [ ] For **2.0 milestone tasks** (as listed in CLAUDE.md): modifications to `lib/*.sh`
      and `stages/*.sh` are authorized when the CLAUDE.md milestone spec explicitly names
      those files in its "Files to modify" list.
- [ ] `--plan` block follows same pattern as `--init` (early exit)
- [ ] New code is in the correct files for the task type (see above)
```

---

## Dead Code Removal

None.

---

## Naming Normalization

None.

---

## Simplification

Route to: **Sr Coder**

### SIM-1: lib/agent.sh exceeds 300-line ceiling

**Observation source:** Drift log — Milestone 0.5 (`lib/agent.sh:1` is 678–681 lines,
more than double the 300-line ceiling in the Code Quality checklist. Pre-existing condition.
Flagged again for a future refactor pass.)

**Verified state:** Confirmed. `lib/agent.sh` is currently 681 lines. The 300-line ceiling
is a stated Code Quality blocker in reviewer.md. The drift log specifically suggested
splitting into a companion file `lib/agent_monitor.sh` as one option.

**Action:** Refactor `lib/agent.sh` to move the file-monitoring and activity-detection
logic into `lib/agent_monitor.sh`. The split point should follow functional cohesion:
- `lib/agent.sh` retains: agent invocation, JSON output parsing, turn counting, timing,
  summary printing, null-run detection entry point
- `lib/agent_monitor.sh` receives: the FIFO monitoring loop, file-change activity detection
  (`_check_file_activity()`), PID-based kill logic, polling-based JSON-mode detection

`lib/agent.sh` must `source` `lib/agent_monitor.sh` at the top. `tekhton.sh` needs a
corresponding `source` line for `lib/agent_monitor.sh`.

The split must not change any external behavior. All existing tests must continue to pass.
Run `shellcheck lib/agent.sh lib/agent_monitor.sh` and `bash -n` on both files after the split.

Target: both files under 350 lines after split.

---

## Design Doc Observations

Route to: **Human via HUMAN_ACTION_REQUIRED.md**

### DDO-1: wc -l portability pattern shared across multiple stage files

**Observation source:** Drift log — Milestone 5 ("`stages/cleanup.sh` and
`stages/coder.sh`/`stages/tester.sh` all use the `wc -l | tr -d '[:space:]'` idiom.
If this pattern ever needs to change for portability, it will need coordinated updates
across multiple stage files.")

**Verified state:** Confirmed. The `wc -l | tr -d '[:space:]'` pattern (or close variants
like `wc -l | tr -d ' '`) appears in: `stages/cleanup.sh`, `stages/plan_interview.sh`,
`stages/plan_followup_interview.sh`, `stages/plan_generate.sh`. No current portability
issue. No immediate action required. Flagging for human awareness in case a future
portability concern surfaces.

**Recommendation for human:** If a portability issue with `wc -l | tr -d '[:space:]'`
is ever identified (e.g., a platform where `wc -l` produces leading spaces or different
output), the fix must be applied consistently across all stage files that use it. Consider
extracting a `_count_lines()` helper in `lib/common.sh` at that point to centralize the
idiom.

---

## Drift Observations to Resolve

The following drift log entries are addressed by this plan:

| Entry | Addressed By |
|-------|-------------|
| [2026-03-16 \| Milestone 5] wc -l portability concern | DDO-1 → human |
| [2026-03-16 \| Milestone 5] `--` separator | Out of Scope (formatting artifact) |
| [2026-03-16 \| Milestone 4] ARCHITECTURE.md missing lib/clarify.sh, lib/replan.sh, CLARIFICATIONS.md | SF-1 → jr coder |
| [2026-03-16 \| Milestone 4] `--` separator | Out of Scope (formatting artifact) |
| [2026-03-16 \| Milestone 1] reviewer.md boundary conflict with v2.0 | SF-2 → jr coder |
| [2026-03-16 \| Milestone 1] Prior Blocker Verification: FIXED | Out of Scope (already resolved) |
| [2026-03-16 \| Milestone 0.5] lib/agent.sh 681 lines | SIM-1 → sr coder |
| [2026-03-16 \| Milestone 0.5] `--` separator | Out of Scope (formatting artifact) |

---

## Out of Scope

**`--` separator lines (3 entries):** These are formatting artifacts in the drift log
between observation blocks from the same pipeline run, not actionable observations.
No remediation needed.

**Prior Blocker Verification: FIXED (Milestone 1 entry):** The drift entry contains an
inline verification note confirming two previously-flagged issues were already fixed
(`lib/context.sh` gaining `set -euo pipefail`, and token format in `print_run_summary()`).
Both fixes are confirmed in the codebase. No further action required. This entry should
be marked RESOLVED when the drift log is next processed.

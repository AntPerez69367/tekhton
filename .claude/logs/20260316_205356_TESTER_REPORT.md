## Planned Tests
- [x] `tests/test_context_compiler.sh` — verify context compiler functions with new file split

## Test Run Results
Passed: 34  Failed: 6

## Bugs Found
- BUG: [lib/context_budget.sh:49] `_filter_block` uses `declare -x "$var_name=$filtered"` inside a function, which creates a local variable; the filtered value is never propagated back to the caller's scope after the function returns
- BUG: [lib/context_budget.sh:132] `_compress_if_over_budget` uses `declare -x "$var_name=$compressed_with_note"` inside a function with the same scoping issue; compression notes and reduced content are discarded when the function returns

## Files Modified
- [x] `tests/test_context_compiler.sh`

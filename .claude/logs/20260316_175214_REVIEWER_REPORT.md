# Reviewer Report — Milestone 5: Autonomous Debt Sweeps (Re-review)

## Verdict
APPROVED_WITH_NOTES

## Complex Blockers (senior coder)
- None

## Simple Blockers (jr coder)
- None

## Non-Blocking Notes
- `lib/notes.sh:207` — `select_cleanup_batch()` references `${PROJECT_DIR}/CODER_SUMMARY.md` directly while the rest of `notes.sh` operates on `HUMAN_NOTES.md` without the `PROJECT_DIR` prefix (relying on CWD). This cross-concern gives `notes.sh` knowledge of a coder-stage artifact; the lookup could alternatively live in `stages/cleanup.sh`, which already knows the project layout.
- `stages/cleanup.sh:59` — `echo "$batch" | wc -l | tr -d '[:space:]'` — same `wc -l | tr -d '[:space:]'` pattern appears in multiple stage files. Harmless today, but a portability change (e.g., macOS vs GNU wc) would need to be applied in several places.
- `prompts/cleanup.prompt.md` — `## Not Attempted` section in the required output format is intentionally left unprocessed by `_parse_cleanup_report()` (items stay open as `[ ]`). A brief comment in `_parse_cleanup_report()` noting this is deliberate would prevent future confusion about the missing case.

## Coverage Gaps
- No test coverage for the targeted revert path in `stages/cleanup.sh`. A test asserting that primary-pipeline file changes survive a cleanup build-gate failure would prevent regression of the core fix from this cycle.
- `count_unresolved_notes()` in `lib/notes.sh` has no test coverage. A test for the absent-file case (returns 0) and the `[DEFERRED]`-exclusion case would complete the batch-selection contract.

## Drift Observations
- `stages/cleanup.sh` and `stages/coder.sh`/`stages/tester.sh` all use the `wc -l | tr -d '[:space:]'` idiom for line counting. If this pattern ever needs to change for portability, it will need coordinated updates across multiple stage files.

---

## Blocker Verification

**Complex Blocker (previous): `git checkout -- .` blanket revert**
- Fixed at `stages/cleanup.sh:77` — pre-cleanup snapshot: `pre_cleanup_files=$(git diff --name-only 2>/dev/null || true)`
- On build-gate failure, lines 110–121 iterate only over post-cleanup changed files and revert a file only if it was NOT present in the pre-cleanup snapshot. Primary pipeline work is preserved. ✓

**Simple Blocker (previous): `AGENT_TOOLS_CLEANUP` unbound variable**
- Fixed at `stages/cleanup.sh:83` — `local _cleanup_tools="${AGENT_TOOLS_CLEANUP:-${AGENT_TOOLS_JR_CODER:-}}"` with a double safe default as a local variable. No unbounded variable access under `set -eu`. ✓
